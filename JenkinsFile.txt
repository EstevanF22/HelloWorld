stage('Deploy to Kubernetes') {
    steps {
       sh kubectl create deployment hello-minikube --image=kicbase/echo-server:1.0
       sh kubectl expose deployment hello-minikube --type=NodePort --port=8080
    }
}
